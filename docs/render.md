# Organizing templates

## Version control
The Archetect `render` command supports generating project templates from a git repository. 
The git repo can also be remote. For e.g.

```aidl
archetect render git@github.com:archetect/archetype-rust-cli.git
```  

## Template catalogs